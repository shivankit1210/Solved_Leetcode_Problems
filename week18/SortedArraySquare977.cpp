class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> square;

        // ******* Optimise method (__PENDING__) *******

        int n =nums.size();
        int left = 0;
        int right = n-1;
        int index = 
        while()


        // ****** brute force ******
        // for(int i=0;i<nums.size();i++){
        //  square.push_back(nums[i]*nums[i]);
        // }
        // sort(square.begin(),square.end());
        // return square ;
        
    }
};